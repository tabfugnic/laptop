#!/bin/bash

source lib/utils.sh

working_directory=`pwd`
support_directory=$working_directory/support

install_dependencies() {
    apt-get update
    apt-get install -y `cat $support_directory/dependencies`
}

runner install_dependencies "Install base packages"
